@page "/fetchdata"

@using MyComponents
@using MyMauiApp.Data
@inject WeatherForecastService ForecastService

<h1>Weather forecast</h1>

<p>This component demonstrates fetching data from a service.</p>

@if (forecasts == null)
{
    <p><em>Loading...</em></p>
}
else
{
	<Grid Items="forecasts">
		<Columns>
			<Column TItem="WeatherForecast" HeaderText="Date" width="300px">
				<ItemTemplate>@context.Date.ToShortDateString()</ItemTemplate>
			</Column>
			<Column TItem="WeatherForecast" HeaderText="Temp. (C)" style="font-weight: bold; text-decoration: underline; color: red;">
				<ItemTemplate>@context.TemperatureC</ItemTemplate>
			</Column>
			<Column TItem="WeatherForecast" HeaderText="Temp. (F)">
				<ItemTemplate>@context.TemperatureF</ItemTemplate>
			</Column>
			<Column TItem="WeatherForecast" HeaderText="Summary">
				<ItemTemplate>@context.Summary</ItemTemplate>
			</Column>
		</Columns>
	</Grid>

}

@code {
    private WeatherForecast[] forecasts;

    protected override async Task OnInitializedAsync()
    {
        forecasts = await ForecastService.GetForecastAsync(DateTime.Now);
    }
}
